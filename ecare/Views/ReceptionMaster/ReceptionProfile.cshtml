
@{
      ViewBag.Title = "ReceptionProfile";
    Layout = "~/Views/Shared/_ReceptionLayoutPage.cshtml";

    if (Session["Email"] != null)
    {
        ViewBag.id = @Session["Id"].ToString();
        ViewBag.Name = @Session["Name"].ToString();
        ViewBag.Email = @Session["Email"].ToString();
    }
}
    <div class="container-fluid">
        <h4 class="mt-4">
            Edit Your profile
        </h4>   <hr />
        <div class="row mt-4">
            <div class="col-lg-12">
                <form>
                    <div class="container">
                        <div class="row">
                            <div class="col-md-8">
                                <div class="form-group">
                                    <label id="eb">Name </label>
                                    <div class="col-md-3">
                                        @Html.TextBox("Name", (string)ViewBag.Name, new { @class = "form-control", @name = "Name" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label id="eb">Email </label>
                                    <div class="col-md-3">
                                        @Html.TextBox("Email", (string)ViewBag.Name, new { @class = "form-control", @Email = "Email" })
                                    </div>
                                </div>
                                @*<label for="email"><b>License Number</b></label>
                                <input type="text"
                                       placeholder="Enter Email "
                                       class="form-control"
                                       name="email"
                                       required />
                                <label for="phone"><b>Phone Number:</b></label>
                                <input type="tel"
                                       max-length="10"
                                       placeholder="Enter phone number"
                                       class="form-control"
                                       name="phone"
                                       required />
                                <label for="psw"><b>Address:</b></label>
                                <textarea name=""
                                          id=""
                                          class="form-control"
                                          placeholder="Your address"></textarea>
                                <label for="Hospital Name"><b> Logo</b></label>
                                <input type="file"
                                       class="form-control-file"
                                       name="fileUpload"
                                       required />
                                <div class="form-group mt-2">
                                    <button type="submit" class="btn btn-primary btn-sm">
                                        Update
                                    </button>
                                    <button type="button" class="btn btn-danger btn-sm">
                                        Cancel
                                    </button>
                                </div>*@
                            </div>
                            <div class="col-md-6 mt-4"></div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>



